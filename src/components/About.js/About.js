import React from 'react'
import "./About.css"

function About() {
    return (
        <div class="about" id="about">
            <h5 className="title">About Me</h5>
            <p className="title-content mr-40">
            Good experience on working with CSS Background, padding, table, Animations, Pseudo classes and Layouts, positioning, border, margin, Pseudo elements and CSS behaviors including the Box model.
            Expertise in creating the user validation forms and using the RESTful services for sending the data to the server.abs
            Used Node Package Manager (NPM) to manage the modules and used it to install useful tools such as  Express, require.js, crypto.js, passport.js, mongoose, mongo.js, body- parser, bcrypt.js and socket.io.
            </p>
        </div>
    )
}

export default About
